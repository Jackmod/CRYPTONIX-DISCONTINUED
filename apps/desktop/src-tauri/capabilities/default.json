{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline window capability plus opening external links in the real browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
